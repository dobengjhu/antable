% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twobytwo.R
\name{twobytwo}
\alias{twobytwo}
\title{Create a contingency table and calculate summary statistics}
\usage{
twobytwo(
  study_tbl,
  outcome,
  exposure = NULL,
  ci_method = c("wald", "clopper-pearson", "agresti-coull", "wilson"),
  test_method = c("z", "chisq", "fisher_exact", "none"),
  alpha = 0.05,
  ...
)
}
\arguments{
\item{study_tbl}{A data.frame or tbl containing the exposure and outcome variables to be analyzed, as specified in the \code{exposure} and \code{outcome} arguments.}

\item{outcome}{A string specifying the binary outcome variable in \code{study_tbl}. By default, the variable's highest level is treated as the "event". To specify a different level as the "event," use the form \code{"var_name == var_level"}.}

\item{exposure}{(Optional) A string specifying the binary exposure variable in \code{study_tbl}. Default is \code{NULL}. If a variable is specified, the variable's highest level is treated as "exposed" by default. To specify a different level as "exposed," use the form \code{"var_name == var_level"}.}

\item{ci_method}{A string specifying the method used to calculate the marginal proportion confidence intervals. Defaults to \code{"clopper-pearson"}. One of:
\itemize{
\item \code{"wald"}: normal approximation interval.
\item \code{"clopper-pearson"}: exact interval based on the binomial
distribution.
\item \code{"agresti-coull"}: adjusted Wald interval with improved coverage.
\item \code{"wilson"}: score interval based on inverting the score test.
}}

\item{test_method}{A string specifying the hypothesis test used to test the difference in conditional proportions. Defaults to \code{"z"}. One of:
\itemize{
\item \code{"z"}: two-sample Z-test using normal approximation.
\item \code{"chisq"}: Chi-squared test.
\item \code{"fisher_exact"}: Fisher's exact test.
\item \code{"none"}: No hypothesis test will be performed.
}}

\item{alpha}{A numeric value strictly between 0 and 1 specifying the significance level for confidence interval calculation and hypothesis tests. Defaults to \code{0.05}.}

\item{...}{Additional arguments passed to internal calculation functions, including:
\itemize{
\item \code{correct}: continuity correction, used when \code{test_method} is \code{"z"} or \code{"chisq"}.
\item \code{digits}: number of decimal places to display, used in \code{\link[=print.summary.anthill]{print.summary.anthill()}}.
}}
}
\value{
An object of class \code{anthill}, which is a list containing:
\itemize{
\item \code{outcome_name}: The name of the outcome column in the original \code{study_tbl}.
\item \code{outcome_var}: The values of the outcome column expressed as a factor.
\item \code{exposure_name}: The name of the exposure column in the original \code{study_tbl} (if \code{exposure} is not \code{NULL}).
\item \code{exposure_var}: The values of the exposure column expressed as a factor (if \code{exposure} is not \code{NULL}).
\item \code{contingency_table}: A list containing:
\itemize{
\item \code{data}: The 2 x 2 \code{table} of counts, cross-tabulating the \code{exposure} and \code{outcome} columns.
\item \code{row_prop}: A matrix of row-wise proportions of \code{data} (if \code{exposure} is not \code{NULL}).
\item \code{col_prop}: A matrix of column-wise proportions of \code{data} (if \code{exposure} is not \code{NULL}).
\item \code{cell_prop}: A matrix of cell-wise proportions of \code{data}.
}
\item \code{ci_method}: The value of \code{ci_method}.
\item \code{p_outcome}: The marginal proportion of observations with the event of interest in \code{data}.
\item \code{p_outcome_ci}: A numeric vector of length 2 (\code{lower}, \code{upper}) giving the 100 * (1 - \code{alpha})\% confidence interval for \code{p_outcome}, calculated using the method specified in \code{ci_method}
\item \code{p_exposure}: The marginal proportion of exposed observations in \code{data} (if \code{exposure} is not \code{NULL}).
\item \code{p_exposure_ci}: A numeric vector of length 2 (\code{lower}, \code{upper}) giving the 100 * (1 - \code{alpha})\% confidence interval for \code{p_exposure}, calculated using the method specified in \code{ci_method} (if \code{exposure} is not \code{NULL}).
\item \code{compare_ci}: A data.frame summarizing the risk difference, risk ratio, and odds ratio comparing the conditional proportions of events across the exposed and unexposed groups. Contains columns \code{measure} (the measure name), \code{estimate} (the point estimate), \code{lower_ci}, and \code{upper_ci} (the 100 * (1 - \code{alpha})\% confidence bounds) (if \code{exposure} is not \code{NULL}).
\item \code{compare_test}: If \code{exposure} is not \code{NULL}, a list containing:
\itemize{
\item \code{test_method}: The value of \code{test_method}.
\item \code{test_summary}: A data.frame summarizing the key findings from \code{test_method}.
\itemize{
\item When \code{test_method} is \code{"z"} or \code{"chisq"}, contains columns \code{statistic} (the test statistic), \code{df} (the degrees of freedom for the test), and \code{p-value} (the two-sided p-value for the test).
\item When \code{test_method} is \code{"fisher_exact"}, contains columns \code{or_estimate} (the estimated odds ratio) and \code{p_value} (the two-sided p-value for the test).
}
\item \code{test_result}: The underlying test object returned by the function corresponding to \code{test_method} (e.g., an \code{htest} object from \code{\link[stats:prop.test]{stats::prop.test()}}).
}
\item \code{alpha}: The value of \code{alpha}.
}
}
\description{
Converts binary variables in a data.frame or tibble into a contingency table,
then calculates relevant one-sample and two-sample summary statistics and
inference artifacts.
}
\examples{
set.seed(42)
dat <- data.frame(
  sex = c("Male", "Female")[rbinom(100, 1, 0.52) + 1],
  disease = c("Sick", "Cured")[rbinom(100, 1, 0.2) + 1],
  smoking_status = sample(c("Current", "Former", "Never"),
                          100,
                          replace = TRUE,
                          prob = c(0.4, 0.15, 0.45))
)

# Basic usage with default exposure/outcome levels
result <- twobytwo(study_tbl = dat,
                   exposure = "sex",
                   outcome = "disease")
summary(result)

# Explicitly specify which level represents "exposed"/"event"
twobytwo(study_tbl = dat,
         exposure = "sex == 'Female'",
         outcome = "disease == 'Cured'")

# Specify confidence interval and/or hypothesis test method
twobytwo(study_tbl = dat,
         exposure = "sex",
         outcome = "disease",
         ci_method = "clopper-pearson",
         test_method = "fisher_exact")

# Skip hypothesis testing entirely
twobytwo(study_tbl = dat,
         exposure = "sex",
         outcome = "disease",
         test_method = "none")
}
