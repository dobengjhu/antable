% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{get_contingency}
\alias{get_contingency}
\title{Create contingency tables}
\usage{
get_contingency(out_var, outcome_name, exp_var = NULL, exposure_name = NULL)
}
\arguments{
\item{out_var}{Factor version of \code{outcome} variable.}

\item{outcome_name}{Name of \code{outcome} variable.}

\item{exp_var}{Factor version of \code{exposure} variable. Defaults to \code{NULL}.}

\item{exposure_name}{Name of \code{exposure} variable. Defaulte to \code{NULL}.}
}
\value{
An object of class \code{antcon}, which is a list containing:
- \code{data}: The 1 x 2 \code{table} of counts tabulating the \code{outcome} column (if \code{exp_var} and \code{exposure_name} are not \code{NULL}) or the 2 x 2 \code{table} of counts, cross-tabulating the \code{exposure} and \code{outcome} columns.
- \code{row_prop}: A matrix of row-wise proportions of \code{data} (if \code{exp_var} and \code{exposure_name} are not \code{NULL}).
- \code{col_prop}: A matrix of column-wise proportions of \code{data} (if \code{exp_var} and \code{exposure_name} are not \code{NULL}).
- \code{cell_prop}: A matrix of cell-wise proportions of \code{data}.
}
\description{
Create contingency tables
}
