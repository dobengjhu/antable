% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confidence_intervals.R
\name{moa_ci}
\alias{moa_ci}
\title{Calculate estimates and confidence interval for risk difference, risk ratio, and odds ratio}
\usage{
moa_ci(ctable, alpha)
}
\arguments{
\item{ctable}{A 2 x 2 \code{table} of counts.}

\item{alpha}{A numeric value strictly between 0 and 1 specifying the significance level for confidence interval calculation and hypothesis tests. Defaults to \code{0.05}.}
}
\value{
A data.frame summarizing the risk difference, risk ratio, and odds ratio comparing the conditional proportions of events across the exposed and unexposed groups. Contains columns \code{measure} (the measure name), \code{estimate} (the point estimate), \code{lower_ci}, and \code{upper_ci} (the 100 * (1 - \code{alpha})\% confidence bounds).
}
\description{
Calculate estimates and confidence interval for risk difference, risk ratio, and odds ratio
}
